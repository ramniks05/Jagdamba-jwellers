PORT=3001
JWT_SECRET=replace-with-a-long-random-secret
ADMIN_PASSWORD=choose-a-strong-password

# Optional: full origin of the API when the frontend is not served with a reverse proxy to the same host.
# Leave unset in local dev (Vite proxies /api to the server).
# VITE_API_URL=https://api.example.com
